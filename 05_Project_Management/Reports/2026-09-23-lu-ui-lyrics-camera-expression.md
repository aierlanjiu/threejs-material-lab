# 『律』界面、歌词、镜头与表情修复

日期：2026-09-23  
Agent：codex

## 完成内容

- 镜头：歌词模式获得镜头控制权。机械歌词关闭自动绕行与动态 FOV；反重力歌词保持正面 30° 视角。歌词换句或方阵重建不再触发重新取景；进入歌词模式时清除尚未结束的镜头过渡。首次加载直接进入反重力模式时也不会被初始化机位覆盖。
- 歌词：六首本地音轨及三条合成曲目共 196 句使用同一播放时钟。行序号区分重复歌词；逐字光标由音频时间驱动，支持暂停、跳播与回退。机械方块按字符时点翻出，反重力方块逐字提亮且长句完整分行呈现。补齐《秋意浓》后段重复副歌与《海鸥》尾段重复句。
- 表情：移除反重力沙盒与主循环对同一头像贴图的重复绘制。29 个头像由主循环统一更新，错开表情相位；播放音乐时保留自然表情循环，手动指定表情仍优先，眨眼、呼吸和眼神跟随照常工作。
- 底部魔方：换句间的上层方块持续做缓慢、小幅的分层堆叠动作，底层基本稳定；减少动效设置下静止。
- 界面：全局使用本地苍耳今楷。桌面与手机布局重新避让顶部播放控件、进度轨、舞台按钮。底部工作台默认收起，可由右下角“调律”按钮或 W 键唤出；参数面板有独立关闭按钮，Esc 可一键收起。

## 验证

- `test/lu/lyric-clock.test.js`：九条曲目 196 句、逐字进度前进与回退、长句在两种模式中完整显示、换句与重建后镜头不变，全部通过。
- `test/lu/ui-layout.test.js`：1440px 桌面和 342px 手机的控件避让、字体、工作台与参数面板的打开/关闭，全部通过。
- `test/live_avatar_motion.test.js`：表情、眼神跟随、眨眼、呼吸与静息镜头，通过。
- `test/test_kinetic_lyrics_e2e.js`：两种歌词模式、252 枚底部方块、三次歌词过渡、缓慢堆叠动作、视角与控制台错误检查，通过。
- `test/lu/main-view-geometry.test.js`：快速配置下主画面几何与抗锯齿回归，通过。
- 截图复核了桌面和 390px 手机的反重力画面。移动端参数抽屉可通过面板内按钮收起。

## 精度边界

内置曲目已有行级时间戳；逐字时间在各行内估计，不能宣称每个字都经过人耳逐字校准。新增的《秋意浓》《海鸥》尾段根据本地音频的自动转写与重复波形位置补齐；建议在最终母带验收时对这些尾段进行听感复核。导入带 `<mm:ss.xx>` 标记的增强 LRC 时会使用显式逐字时点，也可在歌词数据中提供 `charTimes` 精调。

本次修改前的相关文件副本位于 `/Users/papazed/Downloads/lu-fix-pre-20260923-125254/`。保留了仓库中原有未提交改动；未执行删除、发布或部署。
