# Three.js Material Lab

包含：

- `index.html`：可直接运行的 Three.js 单页示例
- `AGENT_PROMPT.md`：可直接交给编码 Agent 的实现/验收指令

运行方式：

```bash
python -m http.server 8000
```

然后浏览器打开 `http://localhost:8000/`。

注意：示例通过 jsDelivr 加载 Three.js ESM，因此需要联网。
